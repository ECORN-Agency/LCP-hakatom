import Timeline, { loader as timelineLoader, headers as timelineHeaders } from "./app.timeline";

export const loader = timelineLoader;
export default Timeline;
export const headers = timelineHeaders;
